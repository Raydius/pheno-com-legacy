block variables
- copy = require('app/copy.js')

nav.topnav(role="navigation")
	.nav-wrapper
		header.logo-wrapper
			a(href="#")
				img(src=copy.logo.src, alt=src=copy.logo.alt).logo

		.tabs
			include menu
