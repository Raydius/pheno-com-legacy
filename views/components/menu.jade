block variables
	- copy = require('app/copy.js')

nav.nav
	ul
		each navItem in copy.navigation
			li(ng-class="{ selected: isSelected('#{navItem.uiSref}') }", class=navItem.className+ ' hvr-underline-from-center')
				a(ui-sref=navItem.uiSref, href=navItem.uiSref, title=navItem.title, ng-click="closeDrawer()")= navItem.navTitle
