.file-input-container(file-input-button="fileModel")
  a.btn.btn-default.attach-res Attach Resume
.attached(ng-repeat="inputfile in fileModel", style="margin-top:20px;")
  .attached-file
    | {{inputfile.name}}
a(ng-show="fileModel.length > 0" href="#",ng-click="removeFiles(inputfile)") X
